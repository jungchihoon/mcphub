{
  "mcpServers": {
    "firecrawl-mcp": {
      "command": "npx",
      "args": [
        "-y",
        "firecrawl-mcp"
      ],
      "env": {
        "FIRECRAWL_API_KEY": "fc-89c11d9ad6ab4636bbfdfff9731d0972"
      }
    }
  },
  "systemConfig": {
    "smartRouting": {
      "enabled": "${SMART_ROUTING_ENABLED:-true}",
      "openaiApiKey": "${OPENAI_API_KEY}",
      "openaiApiBaseUrl": "${OPENAI_API_BASE_URL:-https://api.openai.com/v1}",
      "openaiApiEmbeddingModel": "${OPENAI_EMBEDDING_MODEL:-text-embedding-3-small}",
      "dbUrl": "${DATABASE_URL:-postgresql://localhost:5432/mcphub}"
    }
  }
}